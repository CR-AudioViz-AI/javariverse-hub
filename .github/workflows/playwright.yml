# DISABLED - Replaced by e2e-craudiovizai-playwright.yml
# This workflow is kept for reference only
name: Playwright E2E Tests (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use e2e-craudiovizai-playwright.yml instead.'
        required: true
        default: 'disabled'

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"
